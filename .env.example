POSTGRES_USER=bamp
POSTGRES_PASSWORD=bamp
POSTGRES_DB=bamp

# BR-05 feasibility safety margin (fraction of battery capacity). Provisional — TBC with supervisor.
SAFETY_MARGIN=0.15

# Tier 2 AI explanation (UC-06). Disabled by default; the template fallback is always used when off (BR-07).
AI_EXPLANATION_ENABLED=false
